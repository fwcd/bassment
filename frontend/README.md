# Bassment Frontend

A mobile + web frontend for Bassment, written in React Native.

## Getting Started

To get started developing, run `npm install` to install the dependencies. To run the app for each platform you can use `npm run web`, `npm run ios` and `npm run android`.

> Note: You may need to run `pod install` in the `ios` directory first.
